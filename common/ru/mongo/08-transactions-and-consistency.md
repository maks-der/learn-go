# 8. Транзакции и согласованность

## Описание

Эта тема показывает, как MongoDB держит записи корректными. Запись одного документа всегда атомарна. Мультидокументные транзакции есть на replica set и на шардированных кластерах. Вы узнаёте, когда транзакция не нужна. Задаёте read concern, write concern и read preference. Изучаете причинную согласованность на высоком уровне.

Сначала завершите CRUD, моделирование и драйверы. Завершите эту тему до подробного изучения replica set.

Используйте один термин для каждого понятия. **Атомарный** значит, что запись в один документ полностью происходит или не происходит. **Транзакция** группирует чтения и записи по документам так, что они фиксируются или прерываются вместе. **Write concern** — насколько прочной и насколько широко реплицированной должна быть запись, прежде чем сервер подтвердит её. **Read concern** — насколько актуальным и зафиксированным большинством должно быть чтение. **Read preference** выбирает, с какой реплики читать.

---

## Атомарность одного документа

Запись в **один документ** атомарна. Сервер не оставляет на диске половину полей этой записи. `$set` двух полей в одном `updateOne` применяет оба или ни одного.

Это верно без мультидокументной транзакции. Это первый инструмент согласованности в MongoDB.

Проектируйте под это. Кладите данные, которые должны меняться вместе, в один документ, когда модель позволяет. Заказ со строками в одном документе может обновить `status` и `lines` одной записью.

Атомарность — не изоляция между документами. Два документа могут измениться в порядке, который второй клиент видит как смешанный.

Атомарность сама по себе — не прочность. Запись может быть атомарной в памяти и всё равно пропасть, если узел упадёт до журнала или большинства реплик, в зависимости от write concern.

`updateOne` одного совпадения, `insertOne` и `replaceOne` — однодокументные. `updateMany` атомарна **на документ**, а не как один пакет всё-или-ничего, если нет транзакции.

Не используйте транзакцию только чтобы изменить два поля в том же документе. Одного обновления достаточно.

### Вопросы

#### Теоретические вопросы

1. Что гарантирует атомарность одного документа?
2. Почему заказ со встроенными строками может обойтись без транзакции?
3. Значит ли атомарность, что запись есть на большинстве реплик?
4. Является ли `updateMany` одной атомарной операцией для всех совпадений?
5. Когда транзакция не нужна для двух изменений полей?

#### Простые практические задания

1. Сделайте `$set` двух полей в одном `updateOne`. Найдите документ. Подтвердите, что оба поля изменились.
2. Напишите дизайн встраивания, который держит перевод денег двух балансов **вне** одного документа (это два счёта). Объясните, почему атомарности одного документа недостаточно.
3. Напишите дизайн встраивания, который держит статус и массив журнала **в** одном документе. Запишите одно обновление.
4. Откройте страницу атомарности в руководстве. Запишите URL.

#### Средние практические задания

1. Выполните `updateMany` с `$inc` на трёх документах. Если можете, убейте клиент посередине или обсудите частичное завершение. Запишите, что значит «на документ».
2. Сравните два вызова `$set` и один `$set` с двумя полями. Опишите чередование, которое второй читатель может увидеть в случае двух вызовов.
3. Покажите `$inc` одного поля как однодокументный атомарный счётчик. Запишите, почему клиент не должен читать-прибавлять-писать.

#### Сложные практические задания

1. Прочитайте про конкурентность на уровне документа и `findAndModify`. Запишите одно применение (например захватить документ задания).
2. Спроектируйте корзину как один документ. Запишите, каким шагам оформления всё ещё нужна транзакция (остаток в другой коллекции).

---

## Мультидокументные транзакции

**Мультидокументная транзакция** позволяет читать и писать больше одного документа как одну фиксацию.

Требования:

- **Replica set** (даже из одного члена для учёбы) или **шардированный кластер**
- API драйвера: `startSession`, `startTransaction`, операции, `commitTransaction` или `abortTransaction`
- Одиночный `mongod` без replica set эти транзакции **не** поддерживает

ACID в этом контексте:

- **Atomic** — все записи фиксации появляются или не появляется ни одна
- **Consistency** — согласованность приложения всё равно определяете вы; сервер обеспечивает ограничения, которые вы настроили
- **Isolation** — изоляция снимка для транзакции (прочитайте текущее руководство)
- **Durability** — зависит от write concern при фиксации

Форма примера в `mongosh`:

```javascript
session = db.getMongo().startSession()
session.startTransaction()
const orders = session.getDatabase("shop").orders
const stock = session.getDatabase("shop").stock
orders.insertOne({ _id: 1, sku: "nail", qty: 1 })
stock.updateOne({ sku: "nail" }, { $inc: { qty: -1 } })
session.commitTransaction()
session.endSession()
```

Если запись падает — прервите. Повторите всю транзакцию при временных ошибках (драйвер может помочь).

У транзакций есть лимиты: время, размер и число операций. Не держите транзакцию, пока ждёте пользователя.

Шардированные транзакции поддерживаются в текущих версиях. Они дороже. Держите их короткими.

### Вопросы

#### Теоретические вопросы

1. Почему одиночный `mongod` без replica set отклоняет мультидокументные транзакции?
2. Что значит фиксация против прерывания для двух записей в примере?
3. Почему нельзя ждать пользователя внутри транзакции?
4. Что такое сессия в этом API?
5. Почему шардированная транзакция может стоить больше транзакции replica set?

#### Простые практические задания

1. Подтвердите, что у вас replica set (`hello` или Atlas). Запишите, доступны ли транзакции.
2. Запустите транзакцию на двух коллекциях, которая фиксируется. Найдите оба документа.
3. Запустите транзакцию, которая прерывается. Подтвердите, что ни одна запись не осталась (или прерывание не оставило изменений).
4. Откройте страницу транзакций. Запишите URL.

#### Средние практические задания

1. Вызовите ошибку дубликата ключа внутри транзакции. Прервите. Запишите состояние коллекции.
2. Повторите транзакцию после временной ошибки (или сымитируйте прерывание и повтор). Запишите цикл.
3. Сравните те же две записи без транзакции. Покажите состояние, где есть только одна запись.

#### Сложные практические задания

1. Прочитайте лимиты транзакций (время выполнения, oplog). Запишите два лимита для вашей версии.
2. Запустите транзакцию, которая касается двух шардов (если есть шардированный кластер), или запишите дополнительную осторожность из руководства, если кластера нет.

---

## Когда мультидокументная транзакция не нужна

Мультидокументная транзакция не нужна, когда:

- Хватает записи одного документа (встраивание, вычисленное поле в том же документе)
- Бизнес принимает **конечную** согласованность и задание повтора (например строка журнала, которая может прийти поздно)
- Запись можно сделать **идемпотентной** и можно починить (outbox, повтор)
- Нужен только уникальный индекс, чтобы предотвратить дубликат
- Читаете после записи так, что сессия и majority concern уже покрывают это (см. следующие разделы)

У транзакций есть цена. Они держат блокировки или снимок. Они могут прерваться. Они усложняют код.

Предпочитайте смену модели транзакциям. Если всегда транзакционно обновляете пользователя и счётчик, храните счётчик на пользователе или примите ночную починку.

Не оборачивайте каждый `insertOne` в транзакцию.

Не используйте транзакцию, чтобы спрятать проблему неограниченного массива.

Один `bulkWrite` без транзакции — **не** всё-или-ничего между документами.

Когда транзакция всё же нужна: деньги между двумя счетами в двух документах, остаток плюс заказ, если они разделены, две коллекции, которые не должны показывать смешанное состояние ни одному читателю.

### Вопросы

#### Теоретические вопросы

1. Почему смена модели часто лучше транзакции?
2. Когда конечная согласованность приемлема?
3. Почему `bulkWrite` без транзакции не ACID между документами?
4. Почему «транзакция на каждую вставку» — плохой выбор по умолчанию?
5. Приведите одну предметную область, которой всё ещё нужна мультидокументная транзакция.

#### Простые практические задания

1. Перечислите пять записей в API магазина. Отметьте транзакцию или один документ.
2. Перепишите одно обновление двух документов как один документ. Запишите новую форму.
3. Напишите идемпотентное «создать заказ с этим `orderId`» без транзакции. Используйте уникальный индекс.
4. Составьте таблицу из двух столбцов: «Нужна транзакция» и «Не нужна». Добавьте четыре строки.

#### Средние практические задания

1. Спроектируйте outbox: пишите заказ и событие `outbox` в одной транзакции или покажите однодокументное встраивание outbox. Запишите компромисс.
2. Просмотрите пример сервиса, который использует транзакции для логирования. Предложите убрать. Запишите риск.
3. Покажите конфликт уникального индекса как замену транзакции «проверить, затем вставить».

#### Сложные практические задания

1. Прочитайте официальные рекомендации «когда использовать транзакции». Напишите три предложения с той страницы своими словами.
2. Спроектируйте переводы между счетами. Сравните один документ на счёт плюс транзакцию и коллекцию журнала событий. Напишите одну страницу.

---

## Read concern, write concern и read preference

**Write concern** — правило подтверждения записи.

Частые значения:

- `w: 1` — подтверждает primary. Быстро. Падение primary может потерять запись, если она не реплицировалась.
- `w: "majority"` — большинство голосующих членов replica set сохраняет запись. Безопаснее.
- `j: true` — запись в журнале.

Atlas часто ставит majority по умолчанию. Задавайте majority для денег и для данных, которые нельзя пересобрать.

**Read concern** — правило снимка для чтения.

Частые значения:

- `local` — данные на выбранном узле, даже если они не зафиксированы большинством
- `majority` — данные, которые зафиксировало большинство
- `snapshot` — согласованный снимок, используется в транзакциях

Чтение с `local` на primary всё ещё может увидеть запись, которая позже откатится в редких случаях отказа, если запись не была зафиксирована большинством.

Не смешивайте записи `w: 1` с верой, что данные уже есть на каждом другом узле.

Concern можно задать в URI, на клиенте, на коллекции или на одной операции. Для важных записей будьте явны.

`w: 0` (без подтверждения) — для особых случаев. Не используйте в API, который должен знать, что запись прошла.

**Read preference** выбирает, какой член replica set выполняет чтение.

Частые режимы:

- `primary` — всегда primary. По умолчанию. Самая сильная свежесть на этом узле.
- `primaryPreferred` — primary, если доступен, иначе secondary
- `secondary` — secondary
- `secondaryPreferred` — secondary, если доступен, иначе primary
- `nearest` — член с низкой задержкой, который совпадает с тегами

Чтения с **secondary** могут быть **устаревшими**. Репликация не мгновенна. Приложение может не увидеть запись, которую только что отправило на primary.

Используйте `primary` для чтений, которые должны видеть последнюю запись этого приложения (или используйте сессию с причинной согласованностью, следующий раздел).

Используйте secondary для отчётов, которые принимают отставание, или чтобы снять нагрузку чтения. Измерьте отставание. Не гадайте.

`nearest` всё равно может попасть на secondary. Не используйте `nearest` для чтений «самое свежее».

Read preference — не read concern. Preference выбирает узел. Concern выбирает, насколько зафиксированными должны быть данные на этом узле.

### Вопросы

#### Теоретические вопросы

1. Чего ждёт `w: "majority"`?
2. Какой риск у `w: 1` при отказе?
3. Что значит read concern `majority`?
4. Почему secondary может пропустить запись, которая только что прошла?
5. Чем read preference отличается от read concern?

#### Простые практические задания

1. Вставьте с `{ writeConcern: { w: "majority" } }` на replica set. Подтвердите успех.
2. Найдите write concern по умолчанию для кластера Atlas или вашего replica set. Запишите его.
3. Выполните `hello` или `rs.status()`. Запишите, какой член — primary. Затем задайте read preference `secondary` и выполните find.
4. Составьте таблицу: режим, может быть устаревшим, типичное применение. Добавьте пять режимов read preference.

#### Средние практические задания

1. Сравните задержку `w: 1` и `w: "majority"` на 100 вставках. Запишите оба времени.
2. Вставьте на primary. Сразу прочитайте с `secondary`. Повторяйте, пока не увидите отставание или не запишете, что отставания не видели. Запишите наблюдение.
3. Объясните словами откат записи `w: 1` (primary умирает до репликации). Нарисуйте шкалу времени.

#### Сложные практические задания

1. Прочитайте про `wtimeout`. Вызовите таймаут невозможным `w` (например `w: 5` на наборе из трёх узлов). Запишите ошибку.
2. Прочитайте про максимальную устарелость (`maxStalenessSeconds`). Запишите, как она ограничивает, насколько устаревшим может быть secondary.

---

## Причинная согласованность (обзор)

**Причинная согласованность** значит: если операция B зависит от операции A, каждый читатель с теми же правилами видит A раньше B.

Пример: клиент пишет пост, затем читает список постов. Без осторожности чтение на устаревшем secondary может пропустить пост.

**Сессии** MongoDB могут дать причинную согласованность. После записи сессия хранит cluster time. Следующее чтение в этой сессии ждёт, пока выбранные члены не дойдут хотя бы до этой точки.

По умолчанию: многие драйверы включают причинную согласованность на сессии. Читайте страницу драйвера.

Причинная согласованность — не полная линеаризуемость всей базы для всех клиентов. Другой клиент без сессии в краевых случаях всё ещё может увидеть другой порядок, в зависимости от concern и preference.

Write concern и read concern всё равно выбираете вы. Причинная согласованность плюс majority concern — частая пара для «прочитать свои записи» в сессии приложения.

Не путайте причинную согласованность с мультидокументной транзакцией. Транзакция — другой инструмент. Можно использовать оба.

Правило высокого уровня: используйте одну сессию для цепочки запросов пользователя, когда следующее чтение должно увидеть предыдущую запись, особенно если читаете со secondary.

### Вопросы

#### Теоретические вопросы

1. Что гарантирует причинная согласованность для операций A, затем B?
2. Почему «записать, затем прочитать» может упасть на secondary без сессии?
3. Что сессия хранит для этой цели (на высоком уровне)?
4. Тождественна ли причинная согласованность транзакции?
5. Делят ли все клиенты кластера одну причинную историю?

#### Простые практические задания

1. Откройте страницу причинной согласованности. Напишите определение одним предложением и URL.
2. В драйвере начните сессию, вставьте, найдите той же сессией. Запишите имена API.
3. Опишите сценарий «записать, затем прочитать на secondary» в четырёх шагах. Отметьте, где помогает сессия.
4. Составьте таблицу: инструмент, какую задачу решает: транзакция, write concern, read preference, причинность сессии.

#### Средние практические задания

1. Попробуйте запись на primary и немедленное чтение на secondary без сессии. Затем с сессией. Запишите разницу (или что отставания не увидели).
2. Прочитайте, включает ли ваш драйвер причинную согласованность на сессиях по умолчанию. Запишите значение по умолчанию.
3. Объясните коллеге, почему двум разным серверам приложения нужен общий токен или чтение с primary, чтобы видеть записи друг друга.

#### Сложные практические задания

1. Прочитайте про `afterClusterTime` и operation time. Напишите короткий глоссарий из трёх терминов.
2. Спроектируйте мобильный API: какие конечные точки используют сессию для read-your-writes, а какие принимают устаревшие чтения.

---

## Обзор темы

Эти вопросы не повторяют вопросы в разделах выше. Они покрывают всю тему.

### Вопросы

#### Теоретические вопросы

1. Как атомарность одного документа, мультидокументные транзакции и write concern решают разные задачи?
2. Когда read concern и read preference оба важны для бага «прочитать после записи»?
3. Почему replica set — требование транзакций, но также место, где появляются устаревшие чтения со secondary?
4. Чем причинная согласованность в сессии отличается от изоляции снимка в транзакции?
5. Коллега оборачивает каждую запись в транзакцию и читает с `nearest`. Какими фактами вы пользуетесь на ревью?

#### Простые практические задания

1. Напишите шпаргалку: атомарность одного документа, API транзакции, когда не использовать, `w`/`readConcern`, режимы read preference, причинность сессии.
2. На replica set зафиксируйте одну транзакцию и одну majority-запись. Найдите оба документа.
3. Нарисуйте шкалу времени: запись клиента, primary, secondary, устаревшее чтение, majority-чтение.
4. Отметьте три записи магазина как: один документ, транзакция или асинхронная починка.

#### Средние практические задания

1. Реализуйте перевод между двумя счетами транзакцией и `w: "majority"`. Прервите при недостатке средств. Напишите тесты.
2. Реализуйте тот же перевод как события в одной коллекции журнала (однодокументное добавление). Сравните код и режимы отказа.
3. Настройте одно API-чтение как `primary` + `majority` и один отчёт как `secondary` + `local`. Запишите, почему каждая пара подходит.

#### Сложные практические задания

1. Измерьте долю прерываний и длительность спорной транзакции (два клиента обновляют одни и те же два документа). Запишите числа и смену модели, которая снижает спор.
2. Прочитайте текущую документацию согласованности для вашей основной версии. Напишите политику на одну страницу: значения по умолчанию для write concern, read concern, read preference и сессий.

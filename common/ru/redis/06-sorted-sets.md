# 6. Упорядоченные множества

## Описание

Упорядоченное множество хранит уникальных членов. У каждого члена есть счёт. Redis держит членов в порядке счёта. Эта тема показывает модель счёта, основные команды записи и диапазона, таблицы лидеров и простые скользящие окна.

Завершите эту тему до изучения потоков, битовых карт и других типов.

Используйте один термин для каждого понятия. Член — уникальная строка. Счёт — число двойной точности. Ранг — позиция в упорядоченном множестве (с нуля). Не считайте упорядоченное множество списком. Вы адресуете членов по имени и по счёту, не только вставкой и снятием.

---

## Счёт + член

Упорядоченное множество — множество плюс число на члена. Член уникален. Счёт может повторяться. Два члена могут делить один счёт. Redis тогда упорядочивает этих членов по строке члена (лексикографический порядок).

Счёт — 64-битное число с плавающей точкой. Большие целые могут потерять точность. Для времени многие коллективы хранят время Unix в секундах или миллисекундах как счёт. Для рейтингов они хранят очки.

Нельзя хранить две копии одного члена. Новый `ZADD` того же члена обновляет счёт.

У отсутствующего члена нет счёта. `ZSCORE` возвращает null.

Упорядоченное множество — один ключ. Типичные имена:

```text
lb:game:42
z:recent:user:9
z:delay:emails
```

Выбирайте счёт так, чтобы запросы диапазона совпадали с задачей. Если нужно «топ 10 по очкам», счёт — очки. Если нужно «события за последние 60 секунд», счёт — метка времени.

Стоимость памяти выше, чем у множества. Redis держит dict и skip list (или компактное кодирование для маленьких ключей). Используйте упорядоченное множество, когда нужны порядок или диапазоны. Используйте множество, когда нужна только уникальность.

Не используйте члена как огромный JSON-блоб. Храните id как члена. Храните объект в хеше.

### Вопросы

#### Теоретические вопросы

1. Какие две части данных есть у каждой записи упорядоченного множества?
2. Могут ли два члена иметь один счёт?
3. Что происходит, когда вы делаете `ZADD` существующего члена с новым счётом?
4. Что возвращает `ZSCORE` для отсутствующего члена?
5. Почему хранить id как члена вместо большой строки JSON?

#### Простые практические задания

1. Напишите пять предложений, которые описывают счёт и члена.
2. Составьте таблицу из трёх столбцов: член, счёт, смысл. Добавьте четыре строки для игры.
3. Предложите счёт для «времени последнего входа» и счёт для «общей суммы трат».
4. Откройте страницу типа данных упорядоченного множества. Запишите одно предложение с этой страницы.

#### Средние практические задания

1. Объясните лексикографический порядок, когда счёты равны. Дайте три имени членов и их порядок при счёте `1`.
2. Найдите тип счёта в документации (IEEE double). Запишите один предел для больших целых id как счётов.
3. Нарисуйте маленькую идею skip list (коробки и стрелки) на уровне новичка. Подпишите «упорядочено по счёту».

#### Сложные практические задания

1. Прочитайте, почему Redis использует skip list плюс хеш-таблицу для упорядоченных множеств. Напишите шесть предложений.
2. Спроектируйте членов и счёты для рейсов (цена и время). Запишите, почему могут понадобиться два упорядоченных множества.

---

## `ZADD`, `ZRANGE`, `ZREVRANGE`, `ZRANK`, `ZSCORE`

`ZADD key score member [score member ...]` добавляет или обновляет членов. Ответ — число новых членов (не число обновлений счёта), если не используете опции вроде `CH`.

Опции включают `NX` (добавить только если нет), `XX` (обновить только если есть), `GT` / `LT` (обновить счёт только если больше или меньше) и `INCR` (добавить к счёту, как `ZINCRBY`). Читайте страницу команды для вашей версии.

`ZSCORE key member` возвращает счёт как строку или null.

`ZRANK key member` возвращает ранг от низшего счёта к высшему. У низшего счёта ранг `0`. Отсутствующий член возвращает null.

`ZREVRANK` возвращает ранг от высшего счёта к низшему. У высшего счёта ранг `0`.

`ZRANGE key start stop` возвращает членов по рангу. `0` — низший счёт. `-1` — высший. Добавьте `WITHSCORES`, чтобы включить счёты.

```text
ZADD lb 100 ada 80 bea 80 cam
ZRANGE lb 0 -1 WITHSCORES
ZRANK lb ada
ZSCORE lb bea
```

`ZREVRANGE` возвращает членов от высокого ранга к низкому. Redis 6.2+ предпочитает `ZRANGE ... REV` вместо `ZREVRANGE`. Оба существуют. Новый код может использовать `ZRANGE` с `REV`.

`ZCARD` возвращает число членов. `ZCOUNT key min max` считает членов в диапазоне счёта.

`ZREM key member` удаляет членов. `ZINCRBY key increment member` добавляет к счёту.

Ранги меняются, когда меняются счёты. Не кэшируйте ранг в приложении надолго без обновления.

### Вопросы

#### Теоретические вопросы

1. Что возвращает `ZADD` по умолчанию?
2. Что такое ранг `0` для `ZRANK`?
3. Что такое ранг `0` для `ZREVRANK`?
4. Как включить счёты в ответ `ZRANGE`?
5. Какая современная замена `ZREVRANGE`?

#### Простые практические задания

1. Сделайте `ZADD` трёх членов с разными счётами. `ZRANGE 0 -1 WITHSCORES`. Сохраните ответ.
2. `ZSCORE` одного члена. `ZRANK` того же члена. Сохраните оба.
3. `ZINCRBY` одного члена на `5`. Снова `ZSCORE`.
4. `ZREVRANGE 0 0` (или `ZRANGE REV`). Запишите верхнего члена.

#### Средние практические задания

1. Добавьте двух членов с одним счётом. Покажите порядок `ZRANGE` и `ZRANK` каждого.
2. Используйте `ZADD NX` и `ZADD XX` на одном члене. Запишите оба ответа.
3. `ZRANGE` с `BYSCORE`, если уже прочитали следующий раздел, или используйте ранги, чтобы взять только топ 2.

#### Сложные практические задания

1. Постройте 1000 членов конвейером. Замерьте `ZRANK` одного члена против `ZRANGE 0 -1` в клиенте, чтобы найти ранг. Запишите урок.
2. Проверьте `ZADD GT` / `LT`, если есть. Напишите правило: «обновлять счёт только когда новый счёт лучше».

---

## `ZRANGEBYSCORE`, `ZREMRANGEBYSCORE`

`ZRANGEBYSCORE key min max` возвращает членов, чьи счёты лежат между `min` и `max`. Обе границы включительные по умолчанию. Префикс границы `(` делает её исключительной.

```text
ZRANGEBYSCORE events 1700000000 1700003600
ZRANGEBYSCORE events (10 (20
```

Используйте `-inf` и `+inf` для открытых концов.

`LIMIT offset count` листает диапазон. Всегда используйте `LIMIT` на больших диапазонах.

Redis 6.2+ также позволяет:

```text
ZRANGE key min max BYSCORE
ZRANGE key min max BYSCORE REV
```

Предпочитайте `ZRANGE ... BYSCORE` в новом коде, когда сервер это поддерживает. `ZRANGEBYSCORE` всё ещё работает.

`ZREMRANGEBYSCORE key min max` удаляет членов в этом диапазоне счёта. Ответ — число удалённых членов.

`ZREMRANGEBYRANK key start stop` удаляет по рангу. `ZPOPMIN` и `ZPOPMAX` снимают одного или нескольких членов с концов.

Эти команды реализуют окна времени: счёт — время Unix, `ZRANGEBYSCORE` читает окно, `ZREMRANGEBYSCORE -inf old-cut` сбрасывает старых членов.

Не запускайте `ZRANGEBYSCORE -inf +inf` на огромном ключе без `LIMIT`. Команда может заблокировать и вернуть большой массив.

`ZCOUNT` дешевле, чем доставать всех членов, когда нужен только размер окна.

### Вопросы

#### Теоретические вопросы

1. По чему выбирает `ZRANGEBYSCORE`?
2. Как сделать границу исключительной?
3. Что значат `-inf` и `+inf`?
4. Что возвращает `ZREMRANGEBYSCORE`?
5. Почему нужно добавлять `LIMIT` на больших диапазонах?

#### Простые практические задания

1. `ZADD tw 10 a 20 b 30 c`. `ZRANGEBYSCORE tw 10 20`. Сохраните ответ.
2. `ZRANGEBYSCORE tw (10 30`. Сохраните ответ.
3. `ZCOUNT tw 15 35`. Сохраните ответ.
4. `ZREMRANGEBYSCORE tw -inf 15`. `ZRANGE tw 0 -1`. Сохраните ответ.

#### Средние практические задания

1. Используйте `LIMIT`, чтобы прочитать первых двух членов в диапазоне счёта. Затем прочитайте следующих двух (offset 2).
2. Сравните `ZRANGEBYSCORE` с `ZRANGE ... BYSCORE` на вашем сервере. Запишите, работают ли оба.
3. `ZPOPMAX` дважды. Запишите, какие члены ушли и оставшийся `ZRANGE`.

#### Сложные практические задания

1. Вставьте 10 000 счётов-меток. Удалите всё старше отсечки через `ZREMRANGEBYSCORE`. Замерьте удаление.
2. Прочитайте историю команды `ZRANGE` (BYSCORE, REV, LIMIT). Напишите заметку миграции со старых команд на `ZRANGE`.

---

## Таблицы лидеров

Таблица лидеров — упорядоченное множество id игроков и счётов. Счёт — очки (или время, если меньшее время лучше).

Типичные операции:

- Отправить счёт: `ZADD lb:season1 2500 user:42` или `ZADD ... GT`, чтобы Redis оставлял только лучший счёт
- Прочитать топ 10: `ZREVRANGE lb:season1 0 9 WITHSCORES` или `ZRANGE ... REV`
- Прочитать ранг одного игрока: `ZREVRANK lb:season1 user:42` (добавьте 1 для места с единицы)
- Прочитать счёт: `ZSCORE lb:season1 user:42`
- Прочитать соседей: взять ранг, затем `ZREVRANGE` вокруг этого ранга

Отображаемые имена и аватары не принадлежат члену, если они меняются. Храните `user:42` как члена. Загружайте профиль из хеша.

Когда два игрока делят счёт, документируйте правило ничьей (лексикографический член). Если нужно «первый, кто достиг счёта, побеждает», закодируйте время в счёте (составное число) или используйте второе упорядоченное множество.

Шардирование огромной таблицы лидеров трудно. Одно упорядоченное множество живёт на одном экземпляре (или одном слоте Cluster). Для очень больших игр команды делят доски (по региону, по неделе) или используют уровень приложения.

Истечение всего сезона: задайте TTL на ключе `lb:season1` или удалите ключ в конце сезона. Нельзя задать TTL одному члену в базовом Redis. Удаляйте игрока через `ZREM`.

Не делайте `ZRANGE 0 -1`, чтобы сортировать в клиенте. Используйте ранги и диапазоны.

### Вопросы

#### Теоретические вопросы

1. Какая команда обновляет очки игрока?
2. Как прочитать топ 10 по высшему счёту?
3. Как получить место с единицы из `ZREVRANK`?
4. Почему член — id, а не отображаемое имя?
5. Почему одну гигантскую глобальную таблицу лидеров трудно шардировать?

#### Простые практические задания

1. Постройте доску из пяти игроков. Напечатайте топ 3 со счётами.
2. `ZREVRANK` одного игрока. Запишите место как ранг плюс один.
3. `ZINCRBY` игрока на 50. Покажите новый топ 3.
4. Нарисуйте таблицу: место, член, счёт.

#### Средние практические задания

1. Реализуйте «оставлять только лучший счёт» через `ZADD GT` или сравнение `ZSCORE` в клиенте. Задокументируйте гонку пути из двух команд.
2. Покажите окно из двух игроков выше и ниже данного игрока через арифметику ранга.
3. Создайте `lb:week1` с `EXPIRE` 8 дней. Запишите, как новая неделя использует новый ключ.

#### Сложные практические задания

1. Закодируйте счёт и время в одном double так, чтобы ничьи ломались более ранним временем. Покажите формулу и два примера. Проверьте порядок через `ZREVRANGE`.
2. Напишите список API таблицы лидеров (отправить, топ N, я, вокруг меня) и команду Redis для каждого. Отметьте хеш-теги Cluster, если два ключа должны остаться вместе.

---

## Скользящие окна в стиле временных рядов (простые случаи)

Скользящее окно держит членов, которые принадлежат последним N секундам (или последним N событиям). Упорядоченное множество может смоделировать простой случай.

Окно времени (уникальные id событий):

1. `ZADD window:user:9 <now> event-id`
2. `ZREMRANGEBYSCORE window:user:9 -inf <now - 60>`
3. `ZCARD window:user:9` — счёт за последние 60 секунд

Этот шаблон — ограничение частоты или счётчик «действий за последнюю минуту», когда у каждого события уникальный id. Один член не может появиться дважды. Если один id может повторяться, добавьте уникальный суффикс.

Окна только по счёту без id могут использовать члена как случайный nonce или как `timestamp-seq`.

Потолок событий (последние N элементов): используйте удаления по рангу. После `ZADD`, `ZREMRANGEBYRANK key 0 -<N+1>`, если используете время как счёт и хотите новейшие N. Проверьте направление ранга. Многие коллективы используют `ZADD` плюс `ZCARD` плюс `ZPOPMIN`, пока размер не станет N.

Эта модель — не RedisTimeSeries. Нет агрегации, даунсэмплинга или компактных выборок. Есть точные члены в окне, пока вы их не удалите.

Пределы:

- Каждое событие использует память на члена и счёт
- `ZREMRANGEBYSCORE` на каждый запрос имеет стоимость
- Cluster: все команды `ZADD` и удаления должны попадать в один ключ

Для тяжёлых временных рядов используйте RedisTimeSeries (обзор темы 7) или специализированное хранилище. Для простых ограничений частоты и «недавних id» упорядоченного множества достаточно.

Можно хранить ключ окна с TTL чуть длиннее окна, чтобы простаивающие ключи исчезали.

### Вопросы

#### Теоретические вопросы

1. Что вы храните как счёт в окне времени?
2. Какая команда сбрасывает слишком старых членов?
3. Как посчитать события в окне после обрезки?
4. Почему члены событий должны быть уникальными в упорядоченном множестве?
5. Когда использовать RedisTimeSeries вместо этого шаблона?

#### Простые практические задания

1. `ZADD win 100 e1 120 e2 200 e3`. Удалите счёты ниже 150. `ZRANGE 0 -1`. Сохраните ответ.
2. `ZCARD win` после обрезки. Сохраните ответ.
3. Напишите трёхшаговый алгоритм окна своими словами.
4. Предложите имена ключей для 60-секундных окон на пользователя.

#### Средние практические задания

1. В `redis-cli` или скрипте добавьте пять событий со счётами `TIME` или настенных часов. Обрежьте до 60 секунд. Напечатайте `ZCARD`.
2. Реализуйте последние 5 событий через `ZADD` и `ZREMRANGEBYRANK` или `ZPOPMIN`. Докажите, что `ZCARD` не больше 5.
3. Задайте TTL на ключе окна. Запишите, почему TTL длиннее окна.

#### Сложные практические задания

1. Скрипт из 10 000 `ZADD` плюс обрезка на каждый вызов. Измерьте время на операцию. Запишите, когда шаблон становится слишком медленным.
2. Сравните это окно со списком меток и со строковым счётчиком плюс TTL. Составьте таблицу из трёх строк: точные уникальные id, память, команды.

---

## Обзор темы

Эти вопросы не повторяют вопросы из разделов выше. Они охватывают всю тему.

### Вопросы

#### Теоретические вопросы

1. Чем отличаются диапазоны ранга и диапазоны счёта и какие команды используют каждый?
2. Как таблица лидеров использует `ZREVRANK` и `ZRANGE` вместе?
3. Как скользящее окно использует `ZADD` и `ZREMRANGEBYSCORE` вместе?
4. Что меняет уникальность членов в окне ограничения частоты?
5. Когда выбирать упорядоченное множество вместо списка или множества?

#### Простые практические задания

1. Создайте `lab:z` с четырьмя членами. Запустите `ZRANGE`, `ZREVRANGE` (или `REV`), `ZSCORE`, `ZRANK`. Сохраните таблицу.
2. Напишите шпаргалку: `ZADD`, `ZRANGE`, `ZRANGEBYSCORE`, `ZREM`, `ZREMRANGEBYSCORE`, `ZINCRBY`, `ZCARD`.
3. Нарисуйте одну таблицу лидеров и одно окно времени. Подпишите смысл счёта на каждом рисунке.
4. `ZPOPMAX` один раз на `lab:z`. Покажите оставшихся членов.

#### Средние практические задания

1. Постройте ключ недельной таблицы лидеров и ключ 60-секундного окна действий. Запустите пять команд на каждом. Задокументируйте их.
2. Напишите скрипт, который листает диапазон счёта через `LIMIT`, пока диапазон не пуст.
3. Задокументируйте разрыв ничьих для вашей игры в десяти строках (один счёт, порядок членов или составной счёт).

#### Сложные практические задания

1. Реализуйте отправку, топ 10 и вокруг-меня в небольшой программе. Используйте одно упорядоченное множество и один хеш для имён.
2. Прочитайте `ZUNIONSTORE` / `ZINTERSTORE` в документации. Слейте две недельные доски в `lab:month` с весом 1. Объясните результат.
